export interface LocationCurrentWeather {
    id: number;
    favoriteId: number;
    isFavorite: boolean
    key: string;
    localizedName: string;
    temprature: number;
    weatherText: string;
}